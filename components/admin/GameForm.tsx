"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function GameForm({categories,game}:{categories:any[],game?:any}){const r=useRouter();const[f,setF]=useState<any>({title:game?.title||"",slug:game?.slug||"",shortDesc:game?.shortDesc||"",description:game?.description||"",image:game?.image||"",downloadUrl:game?.downloadUrl||"",version:game?.version||"",fileSize:game?.fileSize||"",developer:game?.developer||"",categoryId:game?.categoryId||"",status:game?.status||"PUBLISHED",isHot:!!game?.isHot,isFeatured:!!game?.isFeatured,seoTitle:game?.seo?.title||"",seoDescription:game?.seo?.description||""});const set=(k:string,v:any)=>setF((x:any)=>({...x,[k]:v}));async function save(e:React.FormEvent){e.preventDefault();const u=game?`/api/admin/games/${game.id}`:"/api/admin/games";const x=await fetch(u,{method:game?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(x.ok){r.push("/admin/games");r.refresh()}else alert("Save failed")}return <form className="card" onSubmit={save}><div className="formGrid">{["title","slug","version","fileSize","developer","image","downloadUrl"].map(k=><div className="field" key={k}><label>{k}</label><input value={f[k]} onChange={e=>set(k,e.target.value)}/></div>)}<div className="field"><label>Category</label><select value={f.categoryId} onChange={e=>set("categoryId",e.target.value)}><option value="">None</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Status</label><select value={f.status} onChange={e=>set("status",e.target.value)}><option>PUBLISHED</option><option>DRAFT</option><option>ARCHIVED</option></select></div><div className="field full"><label>Short description</label><textarea value={f.shortDesc} onChange={e=>set("shortDesc",e.target.value)}/></div><div className="field full"><label>Description HTML</label><textarea value={f.description} onChange={e=>set("description",e.target.value)}/></div><div className="field"><label>SEO title</label><input value={f.seoTitle} onChange={e=>set("seoTitle",e.target.value)}/></div><div className="field"><label>SEO description</label><input value={f.seoDescription} onChange={e=>set("seoDescription",e.target.value)}/></div></div><label><input type="checkbox" checked={f.isHot} onChange={e=>set("isHot",e.target.checked)}/> Hot</label> &nbsp; <label><input type="checkbox" checked={f.isFeatured} onChange={e=>set("isFeatured",e.target.checked)}/> Featured</label><br/><br/><button className="btn gold">Save Game</button></form>}
