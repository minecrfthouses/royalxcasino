"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function SettingsForm({values}:{values:any}){const r=useRouter();const[f,setF]=useState<any>({siteName:values.siteName||"",siteTagline:values.siteTagline||"",homeTitle:values.homeTitle||"",homeIntro:values.homeIntro||"",footerText:values.footerText||"",contactEmail:values.contactEmail||""});async function save(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/admin/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(x.ok){r.refresh();alert("Saved")}}return <form className="card" onSubmit={save}>{Object.entries(f).map(([k,v])=><div className="field" key={k}><label>{k}</label><textarea value={String(v)} onChange={e=>setF((x:any)=>({...x,[k]:e.target.value}))}/></div>)}<button className="btn gold">Save Settings</button></form>}
