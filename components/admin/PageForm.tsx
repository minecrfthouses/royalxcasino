"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function PageForm({page}:{page?:any}){const r=useRouter();const[f,setF]=useState<any>({title:page?.title||"",slug:page?.slug||"",content:page?.content||"",status:page?.status||"PUBLISHED",seoTitle:page?.seo?.title||"",seoDescription:page?.seo?.description||"",schemaType:page?.seo?.schemaType||"WebPage"});const set=(k:string,v:any)=>setF((x:any)=>({...x,[k]:v}));async function save(e:React.FormEvent){e.preventDefault();const u=page?`/api/admin/pages/${page.id}`:"/api/admin/pages";const x=await fetch(u,{method:page?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(x.ok){r.push("/admin/pages");r.refresh()}else alert("Save failed")}return <form className="card" onSubmit={save}><div className="formGrid"><div className="field"><label>Title</label><input required value={f.title} onChange={e=>set("title",e.target.value)}/></div><div className="field"><label>Slug</label><input value={f.slug} onChange={e=>set("slug",e.target.value)}/></div><div className="field"><label>Status</label><select value={f.status} onChange={e=>set("status",e.target.value)}><option>PUBLISHED</option><option>DRAFT</option><option>ARCHIVED</option></select></div><div className="field"><label>Schema type</label><input value={f.schemaType} onChange={e=>set("schemaType",e.target.value)}/></div><div className="field full"><label>Content HTML</label><textarea value={f.content} onChange={e=>set("content",e.target.value)}/></div><div className="field"><label>SEO title</label><input value={f.seoTitle} onChange={e=>set("seoTitle",e.target.value)}/></div><div className="field"><label>SEO description</label><textarea value={f.seoDescription} onChange={e=>set("seoDescription",e.target.value)}/></div></div><button className="btn gold">Save Page</button></form>}
