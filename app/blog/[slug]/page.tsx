import {prisma} from "@/lib/prisma";import {notFound} from "next/navigation";
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await prisma.post.findUnique({where:{slug},include:{author:true}});if(!p||p.status!=="PUBLISHED")notFound();return <main><section className="section"><article className="article"><div className="kicker">Article</div><h1>{p.title}</h1><p className="muted">{p.publishedAt?.toLocaleDateString()} · {p.author.name}</p><div dangerouslySetInnerHTML={{__html:p.content}}/></article></section></main>}
