import type {MetadataRoute} from "next";import{prisma}from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const b=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const[p,g,posts]=await Promise.all([prisma.page.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.game.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.post.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}})]);return[{url:b,lastModified:new Date()},{url:`${b}/games`,lastModified:new Date()},{url:`${b}/blog`,lastModified:new Date()},...p.map(x=>({url:`${b}/${x.slug}`,lastModified:x.updatedAt})),...g.map(x=>({url:`${b}/games/${x.slug}`,lastModified:x.updatedAt})),...posts.map(x=>({url:`${b}/blog/${x.slug}`,lastModified:x.updatedAt}))]}
