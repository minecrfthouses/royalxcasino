import type { ReactNode } from "react";
import {prisma} from "@/lib/prisma";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(["games","download","blog"].includes(slug))notFound();const p=await prisma.page.findUnique({where:{slug},include:{seo:true}});if(!p||p.status!=="PUBLISHED")notFound();return <main><div className="container breadcrumb">Home / {p.title}</div><section className="section"><article className="article"><div className="kicker">Information</div><h1>{p.title}</h1><div dangerouslySetInnerHTML={{__html:p.content}}/></article></section></main>}
