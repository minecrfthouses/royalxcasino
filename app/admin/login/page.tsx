"use client";
export const dynamic = "force-dynamic";import{useState}from"react";import{useRouter}from"next/navigation";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await x.json();if(!x.ok)return setError(d.error||"Login failed");r.push("/admin");r.refresh()}return <div className="login"><form className="loginBox" onSubmit={submit}><h2>Royal X CMS</h2><p>Administrator login</p>{error&&<p>{error}</p>}<div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div><button className="btn gold" style={{width:"100%"}}>Sign in</button></form></div>}
