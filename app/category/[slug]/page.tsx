import {prisma} from "@/lib/prisma";import {notFound} from "next/navigation";import Link from "next/link";
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=await prisma.category.findUnique({where:{slug},include:{games:{where:{status:"PUBLISHED"}}}});if(!c)notFound();return <main><div className="container breadcrumb">Home / Category / {c.name}</div><section className="section"><div className="container"><div className="kicker">Category</div><h1>{c.name}</h1><p className="lead">{c.description}</p><div className="gameGrid">{c.games.map(g=><Link className="gameCard" href={`/games/${g.slug}`} key={g.id}><img src={g.image||"/logo.svg"} alt={g.title}/><div className="gameBody"><h3>{g.title}</h3><p>{g.shortDesc}</p></div></Link>)}</div></div></section></main>}
